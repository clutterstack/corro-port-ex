# Procfile for Node 1 - Corrosion only
# Phoenix runs separately
# Ports: Corrosion API=8081, Gossip=8787
# Using runtime config (editable via NodeLive)

corrosion: ./corrosion/corrosion-mac agent --config ./corrosion/configs/runtime/node1.toml
